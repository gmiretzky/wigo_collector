# Copy this file to .env and fill in your values.
# .env is NOT tracked by git — safe to put real secrets here.

GEMINI_API_KEY=your_gemini_api_key_here

# Optional: override the internal controller URL (default is fine for most setups)
# CONTROLLER_URL=http://wigo-controller:5000
